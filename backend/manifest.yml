applications:
- name: pm-analyzer-backend
  path: .
  buildpack: python_buildpack
  command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
  memory: 512M
  disk_quota: 512M
  # Removed 'routes' section. The route will be provided via 'cf push -r' in GitHub Actions.
