    applications:
    - name: pm-analyzer-backend
      path: .
      buildpack: python_buildpack
      command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
      memory: 512M
      disk_quota: 512M
      routes:
      - route: pm-analyzer-backend-${org}-${space}.cfapps.${region}.hana.ondemand.com # IMPORTANT: Adjust this route to be unique!
    